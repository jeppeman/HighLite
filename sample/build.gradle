apply plugin: 'com.android.application'

android {
    compileSdkVersion rootProject.ext.compileSdkVersion
    buildToolsVersion rootProject.ext.buildToolsVersion

    compileOptions {
        sourceCompatibility = rootProject.ext.sourceCompatibilityVersion
        targetCompatibility = rootProject.ext.targetCompatibilityVersion
    }

    defaultConfig {
        applicationId "com.example.highlite"
        minSdkVersion rootProject.ext.minSdkVersion
        targetSdkVersion rootProject.ext.targetSdkVersion
        versionCode 1
        versionName "1.0"
    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }
}

dependencies {
    testCompile deps.junit
    compile deps.appcompat
    compile deps.android

//    compile 'com.jeppeman:highlite:1.0-SNAPSHOT'
//    apt 'com.jeppeman:highlite-compiler:1.0-SNAPSHOT'
    compile deps.rxjava
    compile deps.rxandroid
    compile project(':highlite')
    annotationProcessor project(':highlite-compiler')
}
